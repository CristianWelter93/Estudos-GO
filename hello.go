package main

import "fmt"

func main() {
    fmt.Println("Todo programa go tem que ter a declaração do package main que é o que vai gerar o arquivo binário.")
	fmt.Println("\nDigitando no terminal 'go run hello.go' o código é executado!")
	fmt.Println("\nSe quisermos criar um arquivo binario do nosso código devemos executar o comando: 'go build hello.go'")
	fmt.Println("\nRepare que diferente do java não é utilizado o ';'")
}

